#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *
 *
 *
 *
 */

char *argstostr(int ac, char **av)
{
	char *ar;
	int i, size;
	
	if (ac == 0 && av == 0)
	{
	return (NULL);
	}
	size = ac - 1;
	ar = malloc(size * sizeof((int));
	for (i = 0; )
	{
	
	}
	return (ar);
}
