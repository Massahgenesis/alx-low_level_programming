#include <stdio.h>

/**
 * main - Prints the program name, and new line
 * @argc: An argument
 * @argv: An argument
 * Return: 0
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
