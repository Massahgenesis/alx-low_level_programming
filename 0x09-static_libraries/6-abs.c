#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
* _abs - Function that prints the absolute value of an integer
* @a: An integer
* Return:0
*/

int _abs(int a)
	{
		if (a < 0)
		{
			return (a * -1);
		}
		else
		{
			return (a);
		}
		{
		return (0);
		}
	}
